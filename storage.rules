rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow any authenticated user to read and write
      // This ensures your drivers can upload their documents
      allow read, write: if request.auth != null;
    }
  }
}
